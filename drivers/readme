This file contains the dymo drivers for cups, patched for the latest c++ compiler.

patch location: https://aur.archlinux.org/dymo-cups-drivers.git

To build:
patch -Np1 -i cups-ppd-header.patch
sudo ./configure
sudo make
sudo make install 